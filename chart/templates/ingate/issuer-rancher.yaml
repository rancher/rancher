{{- if and (eq (include "rancher.useExternalTls" .) "false") (and (eq .Values.gateway.gatewayClass.tls.source "rancher") (eq .Values.ingress.tls.source "rancher")) -}}
apiVersion: cert-manager.io/v1
kind: Issuer
metadata:
  name: {{ template "rancher.fullname" . }}
  labels:
{{ include "rancher.labels" . | indent 4 }}
spec:
  ca:
    secretName: tls-rancher
{{- end -}}
