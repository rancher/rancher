{{- if eq (include "rancher.gatewayEnabled" .) "true" -}}
{{ $defaultGateway := printf "%s-%s" (include "rancher.fullname" .) "gateway" }}
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: {{ include "rancher.fullname" . }}
  labels:
{{ include "rancher.labels" . | indent 4 }}
spec:
  parentRefs:
    {{- if .Values.gateway.createGateway }}
    # If we're creating our own gateway, reference it in the same namespace
    - name: {{ include "rancher.gateway" . }}
      namespace: {{ .Release.Namespace }}
      sectionName: rancher-http
    - name: {{ include "rancher.gateway" . }}
      namespace: {{ .Release.Namespace }}
      sectionName: rancher-https
    {{- else }}
    # Reference to existing gateway listener
    {{- range $section := .Values.gateway.existingGateway.sections }}
    - name: {{ $.Values.gateway.existingGateway.name }}
      {{- if $.Values.gateway.existingGateway.namespace }}
      namespace: {{ $.Values.gateway.existingGateway.namespace }}
      {{- end }}
      sectionName: {{ $section }}
      {{- end }}
    {{- end }}
  hostnames:
    - {{ template "rancher.hostname" . }}
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: {{ include "rancher.fullname" . }}
          port: {{ ternary "443" "80" .Values.service.disableHTTP }}
{{- end }}
