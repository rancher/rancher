#!/usr/bin/env bash

set -ex

echo "-- chart/validate --"

# Check for helm
if [ -z "$(type -p helm)" ]; then
    echo "helm not found. Helm is required to run tests."
    exit 1
fi

helm lint $(dirname $0)/../../build/chart/rancher
