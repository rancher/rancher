#!/usr/bin/env bash

set -ex

echo "-- chart/validate --"

# Check for helm
if [ -z "$(type -p helm_v4)" ]; then
    echo "helm_v4 not found. Helm is required to run tests."
    exit 1
fi

helm_v4 lint $(dirname $0)/../../build/chart/rancher
