package consts

const (
	TelemetrySecretNamespace = "cattle-telemetry-system"

	ProjectFieldKey = "field.cattle.io/projectId" // Works for both label and annotation; with varying but related values
)
